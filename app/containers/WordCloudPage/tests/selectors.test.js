// import { selectWordCloudPageDomain } from '../selectors';

describe('selectWordCloudPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
